#include <iostream>

using namespace std;

int main()
{
   int x;
   int y;
   int z;
   int summa;
   int starpiba;
   int reizinajums;
   int mod;
   double dalijums;
   /*
   x, y, z
   
   2x + 3y + z
   (xy + xz) * 2
   x*2 * (y - z)
   x = 110101;
   y = 10001;
   z = 101011;
   
   summa = x + y;
   starpiba = x - y;
   reizinajums = x * y;
   dalijums = (double)x / (double)y;
   
   cout << "Skaitla " << x << " un " << y << " summa ir " << summa << endl;
   cout << "Skaitla " << x << " un " << y << " starpiba ir " << starpiba << endl;
   cout << "Skaitla " << x << " un " << y << " reizinajums ir " << reizinajums << endl;
   cout << "Skaitla " << x << " un " << y << " dalijums ir " << dalijums << endl;
   cout << "Skaitla " << x << " un " << y << " mod ir " << mod << endl;
   cout <<
   return 0;
}

